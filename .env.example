# Firebase Configuration
# To set up Firebase Authentication:
# 1. Go to https://console.firebase.google.com/
# 2. Create a new project or select an existing one
# 3. Enable Authentication > Sign-in method > Email/Password and Google
# 4. Go to Project Settings > General > Your apps
# 5. Create a Web app if you haven't already
# 6. Copy the Firebase configuration values below

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Note: Copy this file to .env and replace the placeholder values with your actual Firebase config
# The .env file is gitignored for security
